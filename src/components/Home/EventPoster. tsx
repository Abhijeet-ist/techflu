import { BugIcon as QuestionMark } from "lucide-react";

export default function EventPoster() {
  // Generate random positions for glowing dots
  const dots = Array.from({ length: 8 }, (_, i) => ({
    left: `${Math.random() * 100}%`,
    bottom: `${Math.random() * 100}%`,
    animationDelay: `${i * 0.2}s`,
  }));

  return (
    <div className="max-w-2xl mx-auto bg-[#2D1810] min-h-[600px] relative p-6 overflow-hidden">
      {/* Header */}
      <div className="flex justify-between items-start">
        <div className="flex items-center gap-2 text-orange-500 font-bold">
          <div className="flex flex-col">
            <span className="text-2xl">L</span>
            <span className="text-2xl">P</span>
            <span className="text-2xl">U</span>
          </div>
          <div className="text-xs max-w-[120px] leading-tight text-orange-400">
            Transforming Education Transforming India
          </div>
        </div>
        <div className="text-right">
          <span className="text-red-600 font-bold">NAAC</span>
          <div className="text-red-500 font-bold text-2xl">GRADE A++</div>
        </div>
      </div>

      {/* Main Content */}
      <div className="mt-8 relative z-10">
        <h1 className="text-orange-500 text-5xl font-bold italic mb-4">What&apos;s the Buzz</h1>
        <div className="absolute right-0 top-0">
          <QuestionMark className="w-32 h-32 text-orange-500 rotate-12 stroke-[1.5]" />
        </div>
      </div>

      {/* Scroll Content */}
      <div className="relative mt-4 z-10">
        <div className="bg-[#D4B48C] p-6 rounded-lg relative">
          <div
            className="absolute top-0 left-0 w-full h-4 bg-[#B39475]"
            style={{
              clipPath: "polygon(0 0, 100% 0, 98% 100%, 2% 100%)",
            }}
          />
          <p className="text-[#2D1810] text-xl font-medium leading-relaxed mt-2">
            Tech talks, inspiration, and a dash of code magic! It&apos;s not just an event; it&apos;s a vibe. Join the
            tribe of go-getters pushing the boundaries of tech in a chill and collaborative atmosphere.
          </p>
        </div>
      </div>

      {/* Decorative Bottom */}
      <div className="absolute bottom-0 left-0 w-full">
        <div className="relative h-32">
          {/* Wave Effect */}
          <div className="absolute bottom-0 left-0 w-full">
            <svg viewBox="0 0 1200 120" preserveAspectRatio="none" className="w-full h-32">
              <path
                d="M0,0 C150,20 350,0 500,5 C650,10 800,40 1000,30 L1200,0 L1200,120 L0,120 Z"
                className="fill-orange-900/20"
              />
            </svg>
          </div>
          {/* Glowing Dots */}
          {dots.map((dot, i) => (
            <div
              key={i}
              className="absolute w-2 h-2 bg-orange-400 rounded-full animate-pulse"
              style={{
                left: dot.left,
                bottom: dot.bottom,
                animationDelay: dot.animationDelay,
              }}
            />
          ))}
        </div>
      </div>
    </div>
  );
}
